#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char min;

	for (min = 'a'; min <= 'z'; min++)
	{
		putchar(min);
	}
	putchar('\n');

	return (0);
}
