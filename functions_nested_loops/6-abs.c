#include "main.h"

/**
 * _abs - Check mainh
 * @r: An integer number
 * Description: function prints the absolute value
 * Return: 0 if its a absolute value and 1 if its not
 */
int _abs(int r)
{

	if (r >=  0)
		return (r);
	else
		return (r * -1);

}
